# Cloudflare Pages project (not a Worker).
# Build: npm run build
# Output: dist
# Deploy command in Cloudflare must be: npm run deploy
#   (or: npx wrangler pages deploy dist)
# Do NOT use: npx wrangler deploy
name = "theislecheats"
compatibility_date = "2026-09-12"
pages_build_output_dir = "dist"
