# Cloudflare Pages project — deploy with: wrangler pages deploy
# (npm run deploy). Do not use Workers `main`/`[assets]` here; that
# detaches custom domains and produces HTTP 522 for Googlebot.
name = "theisleqrhe"
compatibility_date = "2026-09-12"
pages_build_output_dir = "./dist"
